import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  safeContainer: {
    flex: 1,
    backgroundColor: "#0D0D0D", // Aplica o fundo escuro na área segura
  },
  container : {
    flex: 1,
    width: "100%",
    height: "100%",
    backgroundColor: "#0D0D0D", // Garante que a tela inteira fique escura
  }
});